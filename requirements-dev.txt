# Dependencias de desarrollo y testing.
# Incluye el runtime completo mas las herramientas de test; NO usar en la
# imagen de produccion (mantiene el despliegue liviano). Validado con pytest 9.1.1.
-r requirements.txt
pytest>=8,<10
